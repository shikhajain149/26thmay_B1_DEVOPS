1. to chnage the port number for ssh and then ssh to that machine using new port no
<$sudo vim /etc/ssh/sshd_config>
# Remove and Change port number from line 
<$systemctl restart sshd.service> 
<$ssh -p 23 shikha@192.168.1.7>
